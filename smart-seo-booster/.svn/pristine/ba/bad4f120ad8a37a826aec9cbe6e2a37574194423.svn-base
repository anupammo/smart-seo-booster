/* Smart SEO Booster - WordPress Admin Style Integration */
/* Following WordPress admin design patterns and Google PageSpeed Insights UI */

/* Reset and WordPress Admin Integration */
.smart-seo-wrapper {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.4em;
  color: #2c3338;
  background: #f0f0f1;
  margin: 0;
  padding: 0;
}

/* WordPress Admin Color Scheme */
:root {
  --wp-admin-theme-color: #2271b1;
  --wp-admin-theme-color-darker: #135e96;
  --wp-admin-background: #f0f0f1;
  --wp-admin-white: #ffffff;
  --wp-admin-border: #c3c4c7;
  --wp-admin-border-light: #dcdcde;
  --wp-admin-text: #2c3338;
  --wp-admin-text-light: #646970;
  --wp-admin-success: #00a32a;
  --wp-admin-warning: #dba617;
  --wp-admin-error: #d63638;
  --wp-admin-info: #72aee6;
  
  /* PageSpeed Insights Colors */
  --ps-green: #0cce6b;
  --ps-orange: #ffa400;
  --ps-red: #ff5722;
  --ps-gray: #5f6368;
  --ps-light-gray: #f8f9fa;
  --ps-border: #dadce0;
  --ps-shadow: 0 1px 2px 0 rgba(60,64,67,.3), 0 1px 3px 1px rgba(60,64,67,.15);
}

/* Main Container */
.wrap {
  margin: 10px 20px 0 2px;
  background: var(--wp-admin-background);
  color: var(--wp-admin-text);
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
}

.wrap h1 {
  color: var(--wp-admin-text);
  font-size: 23px;
  font-weight: 400;
  margin: 0 0 16px 0;
  padding: 0;
  line-height: 1.3;
  display: flex;
  align-items: center;
  gap: 8px;
  background: none;
  background-clip: initial;
  -webkit-background-clip: initial;
  -webkit-text-fill-color: initial;
}

.wrap h1:before {
  content: "📊";
  font-size: 24px;
}

/* PageSpeed Insights Style Cards */
.ps-card {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border-light);
  border-radius: 8px;
  box-shadow: var(--ps-shadow);
  margin-bottom: 20px;
  overflow: hidden;
}

.ps-card-header {
  padding: 16px 20px;
  border-bottom: 1px solid var(--wp-admin-border-light);
  background: var(--wp-admin-white);
}

.ps-card-header h2 {
  margin: 0;
  font-size: 16px;
  font-weight: 500;
  color: var(--wp-admin-text);
}

.ps-card-content {
  padding: 20px;
}

/* PageSpeed Score Circle */
.ps-score-circle {
  position: relative;
  width: 96px;
  height: 96px;
  margin: 0 auto 16px;
}

.ps-score-circle svg {
  transform: rotate(-90deg);
  width: 96px;
  height: 96px;
}

.ps-score-circle .score-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 28px;
  font-weight: 600;
  text-align: center;
  line-height: 1;
}

.ps-score-circle .score-label {
  position: relative;
  top: 10px;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 12px;
  color: var(--wp-admin-text-light);
  text-align: center;
}


/* Score Colors */
.score-good { color: var(--ps-green); }
.score-needs-improvement { color: var(--ps-orange); }
.score-poor { color: var(--ps-red); }

/* Metrics Grid */
.ps-metrics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 16px;
  margin-top: 24px;
}

.ps-metric-card {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border-light);
  border-radius: 8px;
  padding: 16px;
  box-shadow: var(--ps-shadow);
}

.ps-metric-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--wp-admin-text);
  margin: 0 0 8px 0;
  display: flex;
  align-items: center;
  gap: 8px;
}

.ps-metric-value {
  font-size: 24px;
  font-weight: 600;
  margin: 0 0 4px 0;
}

.ps-metric-description {
  font-size: 12px;
  color: var(--wp-admin-text-light);
  margin: 0;
}

/* Status Icons */
.status-icon {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 10px;
  font-weight: bold;
}

.status-icon.good { background: var(--ps-green); }
.status-icon.warning { background: var(--ps-orange); }
.status-icon.error { background: var(--ps-red); }

.status-icon.good::before { content: "✓"; }
.status-icon.warning::before { content: "!"; }
.status-icon.error::before { content: "✗"; }

/* Opportunities and Diagnostics */
.ps-section {
  margin-top: 32px;
}

.ps-section-title {
  font-size: 18px;
  font-weight: 500;
  color: var(--wp-admin-text);
  margin: 0 0 16px 0;
  padding-bottom: 8px;
  border-bottom: 1px solid var(--wp-admin-border-light);
}

.ps-audit-list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.ps-audit-item {
  display: flex;
  align-items: flex-start;
  padding: 12px 0;
  border-bottom: 1px solid var(--wp-admin-border-light);
}

.ps-audit-item:last-child {
  border-bottom: none;
}

.ps-audit-icon {
  margin-right: 12px;
  margin-top: 2px;
}

.ps-audit-content {
  flex: 1;
}

.ps-audit-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--wp-admin-text);
  margin: 0 0 4px 0;
}

.ps-audit-description {
  font-size: 13px;
  color: var(--wp-admin-text-light);
  margin: 0;
  line-height: 1.4;
}

/* WordPress Admin Tables */
.wp-list-table {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border);
  border-radius: 0;
  box-shadow: none;
  margin-top: 0;
}

.wp-list-table th {
  background: #f6f7f7;
  border-bottom: 1px solid var(--wp-admin-border);
  color: var(--wp-admin-text);
  font-size: 14px;
  font-weight: 600;
  padding: 8px 10px;
  text-align: left;
}

.wp-list-table td {
  border-bottom: 1px solid var(--wp-admin-border-light);
  color: var(--wp-admin-text);
  font-size: 13px;
  padding: 8px 10px;
  vertical-align: top;
}

.wp-list-table tbody tr:hover {
  background: #f6f7f7;
}

/* WordPress Admin Forms */
.form-table {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border);
  border-radius: 0;
  margin-top: 0;
}

.form-table th {
  color: var(--wp-admin-text);
  font-size: 14px;
  font-weight: 600;
  padding: 20px 10px 20px 0;
  text-align: left;
  vertical-align: top;
  width: 200px;
}

.form-table td {
  color: var(--wp-admin-text);
  font-size: 13px;
  padding: 15px 10px;
  vertical-align: top;
}

.form-table p.description {
  color: var(--wp-admin-text-light);
  font-size: 13px;
  font-style: italic;
  margin: 5px 0 0 0;
}

/* WordPress Admin Buttons */
.button {
  background: #f6f7f7;
  border: 1px solid var(--wp-admin-border);
  border-radius: 3px;
  color: var(--wp-admin-text);
  cursor: pointer;
  display: inline-block;
  font-size: 13px;
  height: 30px;
  line-height: 28px;
  margin: 0;
  padding: 0 12px;
  text-decoration: none;
  white-space: nowrap;
}

.button:hover {
  background: #f0f0f1;
  border-color: var(--wp-admin-theme-color);
  color: var(--wp-admin-theme-color);
}

.button-primary {
  background: var(--wp-admin-theme-color);
  border-color: var(--wp-admin-theme-color);
  color: #fff;
}

.button-primary:hover {
  background: var(--wp-admin-theme-color-darker);
  border-color: var(--wp-admin-theme-color-darker);
}

/* WordPress Admin Notices */
.notice {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border);
  border-left-width: 4px;
  border-radius: 0;
  box-shadow: 0 1px 1px rgba(0,0,0,.04);
  margin: 5px 0 15px 0;
  padding: 1px 12px;
}

.notice.notice-success {
  border-left-color: var(--wp-admin-success);
}

.notice.notice-warning {
  border-left-color: var(--wp-admin-warning);
}

.notice.notice-error {
  border-left-color: var(--wp-admin-error);
}

.notice.notice-info {
  border-left-color: var(--wp-admin-info);
}

.notice p {
  color: var(--wp-admin-text);
  font-size: 13px;
  margin: 12px 0;
}

/* Site Health Style Progress Bars */
.ps-progress-bar {
  background: var(--ps-light-gray);
  border-radius: 4px;
  height: 8px;
  overflow: hidden;
  position: relative;
}

.ps-progress-fill {
  height: 100%;
  transition: width 0.3s ease;
}

.ps-progress-fill.good { background: var(--ps-green); }
.ps-progress-fill.warning { background: var(--ps-orange); }
.ps-progress-fill.error { background: var(--ps-red); }

/* Navigation Tabs (WordPress Style) */
.nav-tab-wrapper {
  border-bottom: 1px solid var(--wp-admin-border);
  margin: 0 0 20px 0;
  padding-top: 0;
}

.nav-tab {
  background: transparent;
  border: 1px solid transparent;
  border-bottom: none;
  color: var(--wp-admin-theme-color);
  cursor: pointer;
  display: inline-block;
  font-size: 14px;
  font-weight: 600;
  line-height: 1;
  margin: 0 5px -1px 0;
  padding: 8px 12px;
  text-decoration: none;
}

.nav-tab:hover {
  background: var(--wp-admin-white);
  border-color: var(--wp-admin-border) var(--wp-admin-border) var(--wp-admin-white);
  color: var(--wp-admin-theme-color-darker);
}

.nav-tab-active {
  background: var(--wp-admin-white);
  border-color: var(--wp-admin-border) var(--wp-admin-border) var(--wp-admin-white);
  color: var(--wp-admin-text);
}

/* SEO Score Display */
.seo-score-display {
  text-align: center;
  padding: 20px;
}

.seo-score-main {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 24px;
  margin-bottom: 24px;
}

/* Dashboard Widget Style */
.dashboard-widget-content {
  padding: 12px;
}

.dashboard-widget-content h4 {
  color: var(--wp-admin-text);
  font-size: 14px;
  font-weight: 600;
  margin: 0 0 12px 0;
}

/* Responsive Design */
@media screen and (max-width: 782px) {
  .ps-metrics-grid {
    grid-template-columns: 1fr;
  }
  
  .seo-score-main {
    flex-direction: column;
    gap: 16px;
  }
  
  .wrap {
    margin: 10px 12px 0 12px;
  }
  
  .form-table th,
  .form-table td {
    display: block;
    width: auto;
    padding: 14px 0;
  }
  
  .form-table th {
    border-bottom: none;
    padding-bottom: 4px;
  }
}

/* Loading States */
.loading-spinner {
  display: inline-block;
  width: 20px;
  height: 20px;
  border: 3px solid rgba(0,0,0,.1);
  border-radius: 50%;
  border-top-color: var(--wp-admin-theme-color);
  animation: spin 1s ease-in-out infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* Accessibility Improvements */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

.screen-reader-text {
  border: 0;
  clip: rect(1px, 1px, 1px, 1px);
  clip-path: inset(50%);
  height: 1px;
  margin: -1px;
  overflow: hidden;
  padding: 0;
  position: absolute;
  width: 1px;
  word-wrap: normal !important;
}

/* WordPress Admin Bar Integration */
#wp-admin-bar-smart-seo-score .ab-item {
  font-weight: 600;
  color: #eee;
}

#wp-admin-bar-smart-seo-score .ab-item .score-value {
  background: var(--ps-green);
  color: white;
  padding: 2px 6px;
  border-radius: 3px;
  margin-left: 4px;
  font-size: 11px;
}

#wp-admin-bar-smart-seo-score .ab-item .score-value.warning {
  background: var(--ps-orange);
}

#wp-admin-bar-smart-seo-score .ab-item .score-value.error {
  background: var(--ps-red);
}

/* WordPress Post/Page List Table */
.column-seo_score {
  width: 80px;
  text-align: center;
}

.seo-score-cell {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
}

.seo-score-value {
  font-weight: 600;
  font-size: 14px;
}

/* WordPress Metabox Styling */
.smart-seo-score-metabox {
  padding: 12px;
}

.smart-seo-score-metabox .ps-score-circle {
  margin: 0 auto 12px;
  width: 64px;
  height: 64px;
}

.smart-seo-score-metabox .ps-score-circle svg {
  width: 64px;
  height: 64px;
}

.smart-seo-score-metabox .score-text {
  font-size: 20px;
}

.smart-seo-score-metabox h5 {
  margin: 12px 0 8px 0;
  font-size: 13px;
  font-weight: 600;
  color: var(--wp-admin-text);
}

.smart-seo-score-metabox ul {
  margin: 0;
  padding-left: 16px;
}

.smart-seo-score-metabox li {
  margin-bottom: 4px;
  font-size: 13px;
  color: var(--wp-admin-text-light);
  line-height: 1.4;
}

/* Dashboard Widget */
.dashboard-widget-content {
  padding: 12px;
}

.dashboard-widget-content .ps-score-circle {
  width: 48px;
  height: 48px;
  margin: 0 auto 8px;
}

.dashboard-widget-content .ps-score-circle svg {
  width: 48px;
  height: 48px;
}

.dashboard-widget-content .score-text {
  font-size: 16px;
}

.dashboard-widget-content table {
  margin-top: 12px;
  width: 100%;
}

.dashboard-widget-content table th,
.dashboard-widget-content table td {
  padding: 6px 8px;
  font-size: 12px;
  text-align: left;
  border-bottom: 1px solid var(--wp-admin-border-light);
}

.dashboard-widget-content table th {
  font-weight: 600;
  background: #f6f7f7;
}

/* Gutenberg Editor Integration */
.smart-seo-panel {
  padding: 16px;
}

.smart-seo-panel .ps-score-circle {
  width: 56px;
  height: 56px;
  margin: 0 auto 12px;
}

.smart-seo-panel .ps-score-circle svg {
  width: 56px;
  height: 56px;
}

.smart-seo-panel .score-text {
  font-size: 18px;
}

.smart-seo-recommendations {
  margin-top: 16px;
}

.smart-seo-recommendations h4 {
  margin: 0 0 8px 0;
  font-size: 14px;
  font-weight: 600;
}

.smart-seo-recommendations ul {
  margin: 0;
  padding-left: 16px;
}

.smart-seo-recommendations li {
  margin-bottom: 4px;
  font-size: 13px;
  line-height: 1.4;
}

/* Help Page Specific Styling */
.smart-seo-help-wrapper {
  max-width: 1200px;
  margin: 0 auto;
}

.help-hero {
  text-align: center;
  padding: 40px 20px;
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border);
  margin-bottom: 20px;
}

.help-hero h2 {
  margin: 0 0 16px 0;
  font-size: 32px;
  font-weight: 600;
  color: var(--wp-admin-text);
}

.help-hero p {
  font-size: 16px;
  color: var(--wp-admin-text-light);
  margin: 0;
}

/* Help Navigation Tabs */
.help-navigation {
  margin-bottom: 20px;
}

.help-tab-content {
  display: none;
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border);
  padding: 20px;
}

.help-tab-content.active {
  display: block;
}

.help-steps {
  margin-bottom: 32px;
}

.step-card {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 20px;
  margin-bottom: 16px;
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border-light);
  border-left: 4px solid var(--wp-admin-theme-color);
}

.step-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background: var(--wp-admin-theme-color);
  color: white;
  border-radius: 50%;
  font-weight: 600;
  font-size: 14px;
  flex-shrink: 0;
}

.step-content h3 {
  margin: 0 0 8px 0;
  font-size: 16px;
  font-weight: 600;
  color: var(--wp-admin-text);
}

.step-content p {
  margin: 0;
  font-size: 14px;
  color: var(--wp-admin-text-light);
  line-height: 1.5;
}

/* Tips and Schema Grids */
.tips-grid,
.schema-types {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 16px;
  margin-top: 20px;
}

.tip-card,
.schema-card {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border-light);
  padding: 16px;
}

.tip-card h4,
.schema-card h3 {
  margin: 0 0 8px 0;
  font-size: 14px;
  font-weight: 600;
  color: var(--wp-admin-theme-color);
}

.tip-card p,
.schema-card p {
  margin: 0;
  font-size: 13px;
  color: var(--wp-admin-text-light);
  line-height: 1.5;
}

/* FAQ and Troubleshooting */
.faq-item,
.trouble-item {
  background: var(--wp-admin-white);
  border: 1px solid var(--wp-admin-border-light);
  padding: 16px;
  margin-bottom: 16px;
}

.faq-item h3,
.trouble-item h4 {
  margin: 0 0 8px 0;
  font-size: 14px;
  font-weight: 600;
  color: var(--wp-admin-text);
}

.solution {
  background: #f0f6fc;
  border: 1px solid #c7d2fe;
  border-left: 4px solid var(--wp-admin-theme-color);
  padding: 12px;
  margin-top: 8px;
}

.solution p {
  margin: 0;
  font-size: 13px;
  color: var(--wp-admin-text);
}

/* Code Examples */
.code-example {
  background: #f6f8fa;
  border: 1px solid var(--wp-admin-border);
  padding: 12px;
  font-family: Consolas, Monaco, monospace;
  font-size: 12px;
  margin-top: 8px;
  overflow-x: auto;
}

/* Responsive Design */
@media screen and (max-width: 782px) {
  .help-hero {
    padding: 20px 16px;
  }
  
  .help-hero h2 {
    font-size: 24px;
  }
  
  .step-card {
    flex-direction: column;
    text-align: center;
    padding: 16px;
  }
  
  .step-number {
    align-self: center;
  }
  
  .tips-grid,
  .schema-types {
    grid-template-columns: 1fr;
  }
  
  .help-tab-content {
    padding: 16px;
  }
}

/* Print Styles */
@media print {
  .nav-tab-wrapper,
  .button {
    display: none !important;
  }
  
  .help-tab-content {
    display: block !important;
    border: none;
    box-shadow: none;
  }
}
